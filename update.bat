@echo off

REM Add all changes
echo "Adding -all changes"
echo "Adding -all changes"
echo "Adding -all changes"
git add --all

REM Commit changes with a default 
echo "Commining -m and Updating the repository"
echo "Commining -m and Updating the repository"
echo "Commining -m and Updating the repository"
git commit -m "Update repository"

REM Push changes to the main branch
echo "Pushing the -u origin into main"
echo "Pushing the -u origin into main"
echo "Pushing the -u origin into main"
git push -u origin main
